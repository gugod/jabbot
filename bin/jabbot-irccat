#!/bin/sh

if [ -z "$*" ] ; then
    echo "Usage:  $0 [network] [channel] [msg]"
    echo "    $0 freenode \"#jabbot\" test";
    exit;
fi

perl -I`dirname $0`/../lib/ -MJabbot::Front::IRC -e 'Jabbot::Front::IRC->cat(@ARGV)' "$@"
