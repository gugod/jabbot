#!/usr/bin/env perl
use common::sense;
use lib 'lib';
use Jabbot::RemoteCore;

my $j = Jabbot::RemoteCore->new;
my $q = join(" ", @ARGV);
my $ans = $j->answers(question => $q);
say "- $_->{content}" for @$ans;
